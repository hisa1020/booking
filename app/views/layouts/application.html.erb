<!DOCTYPE html>
<html>
  <head>
    <title>BookingApp</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="header-inner">
        <div class="header-left-nav">
          <%= link_to image_tag("/assets/logo.png"),"/", alt:"サイトのロゴ画像" %>
          <%= form_with url: search_rooms_path, method: :get, local: true do |f| %>
            <div class="header-sharch d-lg-inline-block d-none">
              <%= f.text_field :area, value:@area, :class => "form-control", placeholder: "エリア" %>
              <i class="fas fa-map-marker-alt"></i>
            </div>
            <div class="header-sharch d-lg-inline-block d-none">
              <%= f.text_field :keyword, value:@keyword, :class => "form-control", placeholder: "キーワード" %>
              <i class="fas fa-search"></i>
            </div>
            <%= f.submit '', class: "d-none" %>
          <% end %> 
        </div>
        <div class="header-right-nav">
          <% if user_signed_in? %>
            <div class="nav-item d-sm-inline-block d-none">
              <%= current_user.name %>
            </div>
            <div class="nav-item">
              <%= image_tag current_user.icon.url, class: "mx-3 p-1 host-icon nav-trigger", alt:"メニュー" %>
              <ul class="header-nav-menu">
                <li>
                  <%=link_to"ルーム登録","/rooms/new"%>
                </li>
                <li>
                  <%=link_to"予約済みルーム一覧",reservations_path%>
                </li>
                <li>
                  <%=link_to"登録済みルーム一覧", posts_rooms_path%>
                </li>
                <li>
                  <%=link_to"設定","/users/account"%>
                </li>
                <li>
                  <%=link_to"ログアウト", "/users/sign_out", method: :delete %>
                </li>
              </ul>
            </div>
          <% else %>
            <div class="nav-item">
            <%= link_to"ログイン", "/users/sign_in", {class: "btn btn-primary my-2 mx-3"} %>
            <%= link_to"登録する", "/users/sign_up", {class: "btn btn-secondary"} %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <div class="clearfix">
    </div>
  
    <script>
      $(function(){
        $('.nav-trigger').click(function(){
          $('.header-nav-menu').toggle(250);
        })
      });
    </script>
    <div class="notifications">
      <%= render "shared/flash" %>
    </div>
    
    <%= yield %>

    <footer class="footer">
      <p>©︎ 2022 POTEPAN.INC.</p>
    </footer>
    
  </body>
</html>
